# Optional: copy to apps/agent/.env. The agent also loads apps/server/.env and the repo-root .env
# if a variable is still unset (paths are fixed; cwd does not matter).
OPENAI_API_KEY=
SERVER_URL=http://localhost:3000
AGENT_ID=default-agent
