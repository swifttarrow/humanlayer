# Task 002: Document Docker Policy Configuration

## Goal
Document how to configure working-directory roots and exposed surfaces for Docker and local runs.

## Deliverables
- [ ] `README.md` includes local vs Docker workdir policy setup instructions
- [ ] `.env.example` and/or `apps/server/.env.example` includes policy-related variables
- [ ] Docs include at least one safe configuration example and one invalid-path example

## Notes
Keep docs aligned with server validation behavior and reason-code semantics.

## Verification
Follow documented setup from a clean environment and confirm create-session behavior matches docs.
